name: Deploy Frontend prod

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-frontend-prod
  cancel-in-progress: true

jobs:
  deploy-prod:
    if: github.repository == 'Richixs/kinetiq-frontend'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image name
        id: image_meta
        run: echo "image_name=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/kinetiq" >> "$GITHUB_OUTPUT"

      - name: Build and push PROD image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.image_meta.outputs.image_name }}:prod-${{ github.sha }}
            ${{ steps.image_meta.outputs.image_name }}:prod-latest

      - name: Notify ArgoCD (PROD)
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.DEPLOY_TOKEN }}
          repository: richixs/gitops
          event-type: update-kinetiq-frontend-prod
          client-payload: |
            {
              "app": "kinetiq",
              "environment": "prod",
              "image": "${{ steps.image_meta.outputs.image_name }}:prod-${{ github.sha }}",
              "sha": "${{ github.sha }}",
              "source_repo": "${{ github.repository }}"
            }
